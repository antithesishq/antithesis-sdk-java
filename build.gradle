allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    archivesBaseName = "antithesis-${it.name}"

    compileJava {
        sourceCompatibility = 1.8
        targetCompatibility = 1.8
    }
}

